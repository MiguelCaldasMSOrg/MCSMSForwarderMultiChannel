name: Deploy stuff to GitHub Pages

# Publishes the legal/ folder as the GitHub Pages site. With this workflow
# the documents are served at the site root, e.g.
#   https://miguelcaldasmsorg.github.io/MCSMSForwarderMultiChannel/privacy-policy.html
#
# To use it: repo Settings → Pages → Source = "GitHub Actions".
# If you prefer the simpler branch deploy (Settings → Pages → Deploy from a
# branch → master → / root), you can delete this file; the pages would then be
# served under the /legal/ path instead.

on:
  push:
    branches: [master]
    paths:
      - "legal/**"
      - ".github/workflows/deploy-legal-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment; don't cancel an in-progress one.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Upload legal/ as the site artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: legal

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
